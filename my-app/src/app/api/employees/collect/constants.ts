/**
 * Constants for employee collection
 */

/**
 * Default number of resumes to collect when fetching employee profiles
 * Adjust this value to control how many employee profiles are fetched in batch operations
 */
export const DEFAULT_RESUMES_TO_COLLECT = 10;

/**
 * Maximum number of resumes that can be collected in a single request
 * This helps prevent excessive API credit consumption
 */
export const MAX_RESUMES_TO_COLLECT = 50;

